# UI references

Visual references for the LightTable-owned workspace and design system:

- `1.png`: start surface with Open, New document and recent files.
- `2.png`: raster editing, tool properties, Layers/Channels/Scopes and Grade.
- `3.png`: vector path editing with Layers and Lens Fx.
- `4.png`: the same vector workflow with Scopes tabbed into a floating panel.

These images show intent and valid Dockview layout variants. Current code,
shared components, design tokens, the panel registry and the persisted
workspace profile remain executable truth; screenshots are not pixel-perfect
contracts.
