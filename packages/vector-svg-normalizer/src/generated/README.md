# Generated SVG normalizer bindings

These files are generated from `crates/vector-svg-normalizer-wasm` by:

```sh
npm run build:vector-svg-normalizer-wasm
```

The build is reproducible and pinned by the crate lockfile and repo-local wasm-bindgen CLI.

