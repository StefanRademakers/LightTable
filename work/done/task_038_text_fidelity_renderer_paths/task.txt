# Task 038 - Text fidelity renderer and text-to-path

Status: complete. Implements Slice 16 from
`architecture/reference/implementation/GPU_TEXT_EXECUTION_TRACKER.md`.

## Production outline route

- [x] Add a deterministic, byte-bounded, submission-pinnable glyph-blob cache
  shared by future web and Electron runtime encoders.
- [x] Expose bounded, scale-independent TrueType/CFF/CFF2 outline commands and
  variable-font locations from the existing lazy Rust/WASM font runtime.
- [x] Transfer outline tables through the versioned, cancellable worker/client
  boundary with strict identity and dedicated-buffer validation.
- [x] Deduplicate concurrent outline requests and retain owned results in a
  byte-bounded LRU keyed independently from font size, transforms and zoom.
- [x] Replace the fixture-only hb-gpu path with the selected bounded alternate
  outline runtime encoder.
- [x] Integrate a device-independent, byte-bounded glyph-outline cache.
- [x] Reuse GPU storage across repeated glyphs and submissions.
- [x] Keep viewport zoom out of content identity and quality invalidation.
- [x] Preserve exact document pixels at extreme viewport zoom.
- [x] Support native solid text fill and stroke through the fidelity route.

## Convert to shape/path

- [x] Convert realized glyph outlines into canonical vector geometry.
- [x] Commit conversion as one explicit undoable document command.
- [x] Preserve the original TextLayer through undo without in-place mutation.
- [x] Expose the command through existing Layers/context and Type menus with an
  editability warning; do not create new UI primitives or CSS.

## Gates

- [x] Focused cache, renderer, command and persistence tests pass.
- [x] Full workspace tests and typechecks pass for the completed renderer work.
- [x] Production web and packaged Electron builds pass for the completed renderer work.
- [x] Physical packaged-Electron high-zoom and save/reopen smoke is recorded:
  `D:\TextTest.psd` retains five editable flow-text layers and one native
  vector/path layer across a fresh process; cursor-focused zoom reaches 1035%
  and visibly preserves document pixels rather than rerasterizing smooth
  viewport vectors.
