# Task 050 - Generic semantic authoring and effect parity

Status: complete

Build imported and native text, shapes, gradients/fills and layer effects on
shared LightTable contracts. Adjustment Layers and the existing Grade panel are
explicitly out of scope and will be handled as a separate parity task.

## Architecture rules

- [x] One reusable paint/gradient asset contract serves text, vector shapes,
  fill layers, Layer Styles and future PDF/AI shading adapters.
- [x] Fill/stroke paint instances own coordinate space and transforms; source
  format descriptors never become renderer authority.
- [x] Semantic layers may retain one bounded derived preview cache with a
  dependency fingerprint. The cache renders only while the semantic source is
  unresolved/unchanged and is invalidated by authoritative edits.
- [x] The same vector scene owns imported and native shapes. Unsupported paint
  or boolean data remains preserved plus preview-backed until promoted.
- [x] Effects are ordered compositing programs with declared bounds and cache
  dependencies. Interactive and final quality use the same semantics.
- [x] Existing LightTable controls, styling and GPU vector overlay are reused.
  Gradient UX follows the compact gradient well plus on-canvas handles pattern;
  no separate custom control family is introduced.
- [x] Every completed capability is committed separately and leaves desktop
  and web builds green.

## Work slices

- [x] Add the generic semantic derived-preview cache and native persistence.
- [x] Treat Photoshop's empty text-path descriptor as a placeholder, preserve
  the raster preview for missing fonts, and promote supported text to editable
  native text.
- [x] Introduce the shared gradient asset/paint-instance model and bridge the
  existing Layer Style gradient editor to it.
- [x] Render and edit gradient fills on vector shapes and text.
- [x] Import Photoshop solid/gradient shapes into the shared vector model,
  including no-fill/no-stroke, alignment, joins, caps and compound paths.
- [x] Add a native Gradient Fill Layer distinct from Adjustment Layers.
- [x] Add effect enable/bypass automation and Photoshop reference captures.
- [x] Implement interactive/final effect quality tiers and improve the highest
  impact fidelity mismatches without changing effect semantics between tiers.

## Gates

- [x] `D:\TextTest.psd` supported text is editable; missing fonts retain their
  source preview until explicit replacement and edits visibly invalidate it.
- [x] `D:\shapes.psd` supported shapes remain editable with fill, no-fill,
  stroke and gradient state round-tripping through native save/reopen.
- [x] Gradient editing stays responsive and exposes on-canvas GPU handles.
- [x] Effect solo/context comparisons include bounds, fill opacity, masks,
  groups, high zoom and stacked effects.
- [x] No Adjustment Layer or Grade-panel behavior changes in this task.

## Completion evidence

- Packaged `D:\TextTest.psd`: five editable flow layers plus one editable path,
  correct rotated thumbnails, active text editing, native save/reopen and zero
  page errors.
- Packaged `D:\shapes.psd`: four editable vectors; fill/no-fill, stroke colour,
  width and alignment edits survive native save/reopen.
- Native Gradient Fill: GPU handle drag and existing gradient editor survive
  native save/reopen as a fifth semantic vector layer.
- Photoshop/LightTable style corpus: five stable source-id targets, context,
  solo, mask/group, fill-opacity, stacked and 400% captures; zero page errors.
