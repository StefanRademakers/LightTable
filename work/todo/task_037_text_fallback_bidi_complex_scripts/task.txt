# Task 037 - Text fallback, bidi and complex scripts

Status: in progress

## Deterministic fallback and provenance

- [x] Build an explicit per-source-run font selection plan from the document
  font snapshot; never let the worker silently choose an unreported face.
- [x] Carry exact/substituted provenance through point and incremental
  paragraph realization and emit a layout warning for every substituted run.
- [x] Lazily materialize the bundled fallback only when a visible document
  actually contains unresolved flow text.
- [x] Keep font registration limited to faces selected by the plan.
- [x] Verify missing, exact and substituted paths with worker/coordinator tests.

## Bidi and complex-script editing

- [x] Move left/right by realized visual caret order while preserving logical
  text storage, selection anchors and grapheme boundaries.
- [x] Verify mixed LTR/RTL selection collapse, Shift extension, Home/End and
  vertical movement.
- [x] Verify Arabic, Hebrew, Devanagari, Thai, CJK, emoji and combining text in
  the real WASM corpus and through save/reopen.
- [x] Defer vertical writing controls until horizontal complex scripts pass.

## Continuous paint performance

- [x] Coalesce native text-colour preview events to at most one canonical
  document paint update per animation frame and always flush the final value.
- [x] Drop superseded text preparation generations before they can enter the
  worker/GPU path, so colour dragging cannot build a serialized render backlog.
- [x] Cover burst supersession with a coordinator regression test.

## Existing UI integration

- [x] Make existing Layers missing/substitution badges actionable using the
  established menu/dialog/control patterns; do not add private widgets/CSS.
- [x] Include missing glyph and substitution details in document/import
  compatibility reporting.

## Gates

- [x] Focused unit, worker and real-WASM tests pass.
- [x] `npm test`, `npm run typecheck`, `npm run build:web` and
  `npm run package:desktop:verify` pass.
- [ ] Web and Electron manual corpus/save-reopen smoke is recorded.
